# AWS Configuration
# IMPORTANT: Use us-east-1 region for Claude 3.7 Sonnet
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-key-here

# Optional: AWS Profile (alternative to access keys)
# AWS_PROFILE=your-profile-name

# Flask Configuration
SECRET_KEY=your-secret-key-for-flask-sessions

# Database Configuration (optional - defaults to SQLite)
# DATABASE_URL=sqlite:///ai_tools.db
# DATABASE_URL=postgresql://user:password@localhost/ai_tools_db
# DATABASE_URL=mysql://user:password@localhost/ai_tools_db

# Development Settings
# SKIP_AWS_VALIDATION=1  # Uncomment to skip AWS validation during development

# Optional: GitHub Token for higher API rate limits
# GITHUB_TOKEN=your-github-personal-access-token

# Optional: Strands Configuration
# STRANDS_MODEL_PROVIDER=bedrock
# STRANDS_MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0
# STRANDS_TEMPERATURE=0.1
EOF < /dev/null
